---
phase: 260803-i1f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles.css
autonomous: true
requirements:
  - QUICK-260803-i1f
must_haves:
  truths:
    - "The testimonial memo panel renders the reference sky blue at exactly rgb(146, 202, 235), equivalent to #92CAEB."
    - "SchoolsToday continues to consume the semantic --memo-section-bg token; no literal target colour is added to component code."
    - "Cream memo cards, tape, typography, spacing, motion, and every other page surface remain unchanged."
    - "Responsive screenshots of the rendered panel are available at 360px, 768px, and 1280px viewport widths outside the repository for evaluator review."
  artifacts:
    - path: "src/styles.css"
      provides: "The exact reference colour through the existing memo-section semantic token"
      contains: "--memo-section-bg: #92CAEB"
    - path: "/private/tmp/tw-testimonial-after-360.png"
      provides: "Untracked 360x900 responsive rendering evidence"
    - path: "/private/tmp/tw-testimonial-after-768.png"
      provides: "Untracked 768x900 responsive rendering evidence"
    - path: "/private/tmp/tw-testimonial-after-1280.png"
      provides: "Untracked 1280x900 rendering evidence comparable with the existing before screenshot"
  key_links:
    - from: "src/components/landing/schools-today.tsx"
      to: "src/styles.css"
      via: "The memo panel background utility resolves var(--memo-section-bg) from :root"
      pattern: "bg-\\[color:var\\(--memo-section-bg\\)\\]"
    - from: "src/styles.css"
      to: "#schools > div"
      via: "Browser computed style resolves the semantic token on the rendered panel"
      pattern: "rgb\\(146, 202, 235\\)"
---

<objective>
Match the SchoolsToday testimonial memo panel's blue field to the attached
sky-blue reference by changing its existing semantic token to #92CAEB.

Purpose: Correct the visible colour mismatch while preserving every other
design and interaction dimension of the testimonial section.

Output: A one-line token update in `src/styles.css`, a browser assertion of the
exact computed RGB, and responsive screenshots stored outside the repository.
</objective>

<execution_context>
@/Users/jeongwondo/.codex/gsd-core/workflows/execute-plan.md
@/Users/jeongwondo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/styles.css
@src/components/landing/schools-today.tsx

<interfaces>
The source reference is
`/var/folders/97/5tm4ps7n7q9_bvl7t0ss0j1r0000gn/T/codex-clipboard-09d0304e-acd9-4bb2-bb0c-5c06d4363224.png`.
Its dominant blue field is the locked target `rgb(146, 202, 235)`, hex
`#92CAEB`.

The current 1280px before screenshot is
`/private/tmp/tw-testimonial-before-1280.png`. Keep it intact for comparison.

From `src/styles.css`:
- `:root` currently defines `--memo-section-bg: #cfe6ff`.
- `--memo-bg` separately controls the cream memo cards and must not change.
- The tape colours use `--memo-tape-1`, `--memo-tape-2`, and
  `--memo-tape-3`; none is in scope.

From `src/components/landing/schools-today.tsx`:
- `SchoolsToday` renders `<section id="schools">`.
- Its direct child panel uses
  `bg-[color:var(--memo-section-bg)]`, so `#schools > div` is the stable
  rendered element for computed-style verification.
- The component file is context only and must remain unmodified per TOK-1.
</interfaces>
</context>

<dependency_graph>
The single task needs the existing `--memo-section-bg` definition and its
SchoolsToday consumer. It changes the token, then verifies source scope,
project health, exact browser resolution, and responsive evidence. It creates
no interface, dependency, or follow-on task.
</dependency_graph>

<source_audit>

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Match the testimonial memo panel to the attached sky-blue reference and verify the rendered colour | 01 | COVERED | Task 1 updates and proves the rendered token |
| REQ | QUICK-260803-i1f | Atomic colour correction with responsive evidence | 01 | COVERED | One production file and one focused task |
| RESEARCH | — | No research artifact or external dependency is required | — | N/A | Existing token/component/browser patterns are sufficient |
| CONTEXT | FACT-01 | Use exact target #92CAEB / rgb(146, 202, 235) | 01 | COVERED | Source and browser assertions |
| CONTEXT | FACT-02 | SchoolsToday memo field is the structurally matching surface | 01 | COVERED | Existing `#schools > div` consumer retained |
| CONTEXT | FACT-03 | Preserve TOK-1 semantic-token ownership | 01 | COVERED | Literal remains in `src/styles.css` only |
| CONTEXT | FACT-04 | Change no other visual or interaction dimension | 01 | COVERED | Single declaration edit and scoped diff review |
| CONTEXT | FACT-05 | Run project tests/build and capture 360/768/1280 screenshots outside git | 01 | COVERED | Automated verification sequence and `/private/tmp` outputs |

</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Match and verify the testimonial memo panel sky blue</name>
  <files>src/styles.css</files>
  <action>
    In the `:root` memo-token group, replace the value of
    `--memo-section-bg` with the exact locked reference hex `#92CAEB`. Make no
    other source edit. In particular, do not change `--memo-bg`, any tape
    token, typography, spacing, motion, card styles, or any unrelated colour.
    Leave `src/components/landing/schools-today.tsx` untouched so the panel
    continues to resolve its background through
    `bg-[color:var(--memo-section-bg)]` per TOK-1. Do not broaden this task into
    cleanup of existing raw colours.

    Inspect the scoped diff immediately after editing. It must show one
    declaration-value replacement in `src/styles.css`; if any other hunk is
    present, remove that task-created change while preserving all pre-existing
    user work.

    Run the full Vitest suite and production build. Then start `pnpm dev` in a
    separate PTY and wait for `http://127.0.0.1:3000/` to respond. Use one
    named `agent-browser` session, set light colour scheme plus reduced motion
    for deterministic captures, and open the page. At each of the following
    viewport sizes, scroll `#schools` to the start of the viewport, assert
    `getComputedStyle(document.querySelector("#schools > div")).backgroundColor`
    equals `rgb(146, 202, 235)`, wait for layout to settle, and capture the
    viewport to the specified path:

    - `360 900` -> `/private/tmp/tw-testimonial-after-360.png`
    - `768 900` -> `/private/tmp/tw-testimonial-after-768.png`
    - `1280 900` -> `/private/tmp/tw-testimonial-after-1280.png`

    Use `agent-browser screenshot PATH` without `--full` so each file records
    the named viewport dimensions. The existing
    `/private/tmp/tw-testimonial-before-1280.png` is comparison input and must
    not be overwritten. Inspect the three new captures against the reference
    and 1280px before image: the outer memo field should take on the reference
    sky blue while the cream cards, tape, copy, layout, and responsive behavior
    remain visually unchanged. Close the browser session and dev server after
    capture. Keep every screenshot under `/private/tmp`; do not add screenshot
    artifacts to git.
  </action>
  <verify>
    <automated>
      `rg -n --fixed-strings -- "--memo-section-bg: #92CAEB;" src/styles.css`
      returns exactly the updated token declaration, and
      `rg -n --fixed-strings "bg-[color:var(--memo-section-bg)]" src/components/landing/schools-today.tsx`
      confirms the semantic consumer is intact. A case-insensitive fixed-string
      count for `#92caeb` under `src/` equals one.

      `pnpm test &amp;&amp; pnpm build` passes.

      With the dev server running, each viewport run uses
      `agent-browser --session memo-colour eval --json
      "getComputedStyle(document.querySelector('#schools > div')).backgroundColor"`
      piped to `jq -e '.data.result == "rgb(146, 202, 235)"'`; the assertion
      passes at widths 360, 768, and 1280. `test -s` passes for all three
      `/private/tmp/tw-testimonial-after-*.png` files, and `file` reports their
      dimensions as 360x900, 768x900, and 1280x900 respectively.
    </automated>
  </verify>
  <done>
    - `--memo-section-bg` is exactly `#92CAEB` and the target literal occurs once under `src/`.
    - `SchoolsToday` still resolves the panel through the semantic token and has no diff.
    - Browser-computed background colour is exactly `rgb(146, 202, 235)` at all three requested widths.
    - Full project tests and production build pass.
    - Three non-empty, correctly sized responsive screenshots exist in `/private/tmp` and none is tracked by git.
    - The production diff contains exactly the intended token-value replacement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Semantic design token -> public rendered UI | The centralized `--memo-section-bg` value flows through a Tailwind arbitrary-value utility into the visible testimonial panel. No user input, data, authentication, or external service crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260803-i1f-01 | Tampering | `--memo-section-bg` and its `SchoolsToday` consumer | low | mitigate | Limit the production diff to the existing token declaration, retain the semantic consumer, assert the exact source literal count, and verify the browser-computed RGB at all requested widths. |
| T-260803-i1f-02 | Denial of Service | Landing-page build/runtime | low | mitigate | Run the full Vitest suite and production build; introduce no dependency, selector rewrite, runtime logic, or asset. |
</threat_model>

<verification>
- The scoped source diff is a single value change in `src/styles.css`.
- Source checks prove the target hex exists once and the component still uses `--memo-section-bg`.
- `pnpm test` and `pnpm build` pass.
- Browser assertions return `rgb(146, 202, 235)` at 360px, 768px, and 1280px widths.
- `/private/tmp/tw-testimonial-after-{360,768,1280}.png` are non-empty PNGs with the requested viewport dimensions and remain untracked.
- Visual inspection against the supplied reference and current 1280px before capture finds no change beyond the outer blue field.
</verification>

<success_criteria>
- The testimonial memo panel visibly matches the reference's exact sky blue.
- The semantic-token architecture remains intact and the component contains no copied colour literal.
- All surrounding testimonial visual and motion details are preserved.
- Automated project and computed-style checks pass, with responsive evidence ready for evaluator review.
</success_criteria>

<output>
After completion, create
`.planning/quick/260803-i1f-match-the-testimonial-memo-panel-backgro/260803-i1f-SUMMARY.md`
summarizing the one-line token diff, test/build results, exact computed colour,
responsive screenshot paths, and confirmation that screenshots were not
committed.
</output>
