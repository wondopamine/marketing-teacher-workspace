---
context: default
phase: ga-landing-wayfinder
task: 1
total_tasks: 2
status: paused
last_updated: 2026-08-13T14:21:50Z
---

# Teacher Workspace GA landing Wayfinder handoff

<current_state>

The approved Wayfinder map is live as GitHub issue #5 with native child issues
#6–#13 and dependency gates. The repository now carries the approved seven-part
direction, an empty capability-owner claims register, explicit rejected claims,
positive-scenario rules, anonymous measurement foundations, and the safe CMS
update policy.

The current Xiao Ming bursary CMS candidate remains untouched so all review
comments and stable targets stay intact. ADR-0004 supersedes that story for the
next candidate. The revised greyscale wireframe is intentionally blocked until
issues #6–#11 provide approved inputs.

</current_state>

<completed_work>

- Created `wayfinder:map`, `wayfinder:grilling`, `wayfinder:prototype`,
  `wayfinder:research`, and `wayfinder:task` labels.
- Published the map and eight native child issues with GitHub dependencies.
- Wrote `docs/landing-page-ga-spec.md` and the approved design decision record.
- Added ADR-0004 and marked the bursary ADR as superseded.
- Added `src/content/landing-ga-foundation.ts` with tests for IA, naming, claim
  exclusions, scenario safety, measurement privacy, prototype gates, and CMS
  mutation safety.
- Updated the existing measurement contract to anonymous CTA conversion plus
  section reach and capability engagement.
- Passed 335 tests, typecheck, scoped lint, build, public-output scanning, and
  route-isolation verification.

</completed_work>

<remaining_work>

1. Resolve the three current frontier tickets: #6 GA claims, #10 Posts proof,
   and #11 measurement.
2. Then resolve #7 hero screen, #8 positive scenarios, and #9 school assurance.
3. Only after those inputs are approved, implement #12 as one revised greyscale
   candidate and one append-only shared CMS save.
4. Verify all comments and statuses, then run the #13 human approval gate.

</remaining_work>

<blockers>

- Capability owners have not confirmed the GA claims register.
- No hero screen, three scenarios, school assurance, Posts quote, or analytics
  owner/thresholds are approved yet.
- `CMS_TEST_EDIT_KEY` and the CMS test database configuration are unavailable,
  so the two database-backed verification scripts could not run locally.

</blockers>

## Critical rules

- Do not run `cms:import`, publish, restore, migrate, reset, or replace the page.
- Do not change the released `/` route or production aliases.
- Before a future CMS save, record every comment ID, target, subject, status,
  and version. The observed baseline is six open comments and one withdrawn.
- Use the current `expectedHead`, abort on conflict, and preserve all stable IDs.
- Keep comments open until Xingyu or Lay Hui confirms resolution.
- Do not reintroduce circular reading, automatic bursary matching, application
  outcome tracking, or student-context drafting without new owner evidence.

<next_action>

Use issue #6 to collect one signed-off claim record per capability. In parallel,
use #10 for Posts quote permission and #11 for the anonymous measurement
denominator, thresholds, owner, cadence, consent, and retention decisions. Do
not edit or save the revised wireframe before these frontier inputs are real.

</next_action>
