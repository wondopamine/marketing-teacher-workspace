---
context: default
phase: platform-copy-review
task: 1
total_tasks: 2
status: paused
last_updated: 2026-08-13T06:12:53.517Z
---

# Platform copy review handoff

<current_state>

The landing-page copy has been rewritten and committed as `394d437`. It now presents Teacher Workspace as a platform across Student Insights, AI next-step guidance, Message drafting, and Posts. The synthetic Xiao Ming bursary journey remains one worked example rather than the whole product story.

The user opened `http://localhost:3000/content-review` and asked to review the copy before it is applied to the shared CMS candidate. The shared CMS database, colleague comments, private review version, and released public homepage have not been changed.

</current_state>

<completed_work>

- Installed and applied the Humanizer skill with the TFX copy guidance.
- Rewrote metadata, hero, story, reveal, capabilities, and closing copy.
- Updated the CMS homepage template while preserving every stable content and review-target ID.
- Updated reviewer rationale and review questions.
- Updated route verifiers and tests for the new copy.
- Passed 326 tests, typecheck, scoped lint, formatting, and the built route/output guards.
- Committed the feature work as `394d437 fix(content): present Teacher Workspace as a platform`.

</completed_work>

<remaining_work>

- Wait for the user to approve or revise the new copy.
- Before changing the shared CMS draft, take a read-only baseline of all existing comment IDs.
- Apply the approved copy through one append-only CMS draft save using the current head and existing stable IDs.
- Verify every baseline comment still exists and remains attached to its original target.
- Only then deploy a fresh preview from this branch. Do not merge or promote it to production.

</remaining_work>

<decisions_made>

- The hero now describes student support and family communication together; it no longer promises that every student will receive support.
- Student Insights is a starting point, not the whole platform.
- The bursary journey is one concrete example across the four public capabilities.
- Comments remain outside page snapshots. Copy updates must preserve stable IDs and append a new draft version.
- A normal branch push is safe because the deployment workflow does not run `cms:import` or database migrations.
- The released `/` route remains unchanged until the later, separately approved public launch.

</decisions_made>

<blockers>

- User approval of the revised copy is required before applying it to the shared CMS draft.
- `CMS_TEST_DATABASE_URL` is not configured locally, so the dedicated PostgreSQL comment-retention integration test was not rerun in this session. The test exists at `src/db/content-repository.integration.test.ts` and covers edits, reorder, archive, and restore.

</blockers>

## Required reading

1. `.planning/HANDOFF.json` — machine-readable state and exact safe next action.
2. `src/cms/templates/homepage-v1.server.ts` — revised canonical template and unchanged stable IDs.
3. `src/db/content-repository.server.ts` — append-only draft save and review-target synchronization.
4. `src/db/content-repository.integration.test.ts` — durable comment-retention contract.
5. `content/landing/02-hero.mdx`, `03-story.mdx`, `05-capabilities.mdx`, and `10-close.mdx` — source copy under review.

## Critical anti-patterns

- Do not run `pnpm cms:import`: it is an initial-page import, not an update path.
- Do not replace, renumber, or regenerate stable section, item, screen, field, or review-target IDs.
- Do not publish, restore, migrate, reset, or otherwise mutate the shared CMS while preparing the preview.
- Do not treat the source template change as if it updated the existing CMS page. The current database page changes only through a versioned save.

## Infrastructure state

- Local route reviewed: `http://localhost:3000/content-review`.
- Local preview server should be stopped before the computer restart.
- Shared CMS database: untouched during this copy task.
- Colleague comments: untouched; no comment write or status update was performed.
- Production homepage: unchanged.
- Branch: `codex/landing-ia-content-structure`.

<context>

The safest continuation is deliberately two-step: approve the words locally, then save them as a new shared CMS draft while proving the existing comments survived. A code push alone does not update the database copy and does not delete comments.

</context>

<next_action>

Ask the user whether the local copy is approved. If yes, record a read-only baseline of existing comment IDs, apply a single append-only CMS draft version with unchanged stable IDs, and verify the baseline comments before deploying a fresh branch preview. Never run `cms:import` for this update.

</next_action>
