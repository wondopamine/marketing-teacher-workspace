---
# Prototype evidence for the greyscale wireframe. Every existing state below
# was checked against the Teacher Workspace design prototype by driving the
# running prototype, and each brief records the build state its capture came
# from. The named people and records are synthetic prototype data.
label: Prototype evidence

# Where the story claims an interface the product does not have yet, the
# annotation carries a question for the PM. Add one by writing
# "pendingInterface-<item id>:" with the question as its value.
pendingInterfaceLabel: Owner confirmation needed
pendingInterface-hero: "Approve the existing Rachel Wong Mei Ling profile and crop for the hero product peek."
pendingInterface-story-promise: "Approve publishing an observation note and its positive tag. The capture is the default build, so no flag decision is needed."
pendingInterface-story-notice: "Approve the existing Rachel Wong Mei Ling wellbeing fields and crop for public synthetic use."
pendingInterface-story-next-steps: "This screen needs a Release 2 capability flag, so teachers will not have it at GA. Confirm whether the landing page may show it, and with what wording."
pendingInterface-story-words: "Confirm that the existing AI Draft template picker represents GA behaviour and is cleared for public use."
pendingInterface-story-family-and-record: "Confirm the exact delivery information Posts may describe publicly at GA. The capture is the overview tile only; the recipient table holds parent contact details and is excluded."
---

<Item id="hero" label="Existing screen · default build">

## Student Insights profile for Rachel Wong Mei Ling

Use the existing synthetic profile for Rachel Wong Mei Ling. It is already a positive, non-support example and needs no new dashboard, tag, or content state. The capture is the default build, so every field shown is one a teacher has today.

- Rachel Wong Mei Ling, Class 3B, Swimming; attendance 100%, conduct Excellent
- Attendance, Behaviour, Wellbeing, Academic, and Family sections
- Overall % and the trend tiles are excluded: both need a capability flag

</Item>

<Item id="story-promise" label="Existing screen · default build">

## An observation a colleague already recorded

Open the Observations panel on the existing profile. It is present in the default build and already carries a positive tag and a colleague's note, so the story does not need an invented attention tag or a new panel.

- Observations panel: 1 observation, 1 teacher, 1 context
- Positive tag “Collaboration”, attributed to the recording teacher
- “Add my observation” action for the teacher reading the profile

</Item>

<Item id="story-notice" label="Existing screen · default build">

## Wellbeing signals already on the profile

The wellbeing section brings signals that today sit in separate systems into one view. Every field is in the default build; none is a projection.

- TCI risk indicators, shown as a count out of five
- Low mood flagged across two or more terms
- Social links, the student's recorded peer connections

</Item>

<Item id="story-next-steps" label="Existing screen · needs a Release 2 flag">

## A suggested next step, written as encouragement

This guidance screen exists and is authored, but it only opens when a Release 2 capability flag is on, so teachers will not have it at general availability. The capture is one card; the surrounding screen frames the student through support classifications and is excluded.

- Suggested action with the reasoning that produced it
- Named colleague to contact, and the reference guide behind the step
- Framed as progress to sustain, not a deficit to correct

</Item>

<Item id="story-words" label="Existing screen · default build">

## AI Draft offers a template, the teacher keeps the pen

Use the existing AI Draft picker in the Posts composer. It offers four authored templates and states plainly that the draft is for the teacher to review and edit before posting. It works from a template, not from a student's record.

- Four templates, each with the response it asks parents for
- “We'll draft the post for you to review and edit before posting”
- Cancel and Draft post actions; nothing sends without the teacher

</Item>

<Item id="story-family-and-record" label="Existing screen · default build">

## The post is delivered, and reading is visible

Use the overview tile from the existing posted letter. It shows what Posts can honestly claim: the message reached families and some have opened it. It does not claim any step beyond delivery.

- Overview showing 2 of 3 read, with 1 unread
- No claim about what a family does after reading
- The recipient table is excluded: it holds parent contact details

</Item>
